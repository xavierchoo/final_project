
<%= button_to "All Articles", articles_path, method: :get %>

 <% @articles.each do |article| %>
    <%= article.name %></br>
    <%= article.description %></br>
    <%= button_to "Read article", admin_selections_path(testing_id: article.id), method: :post %>
<% end %>
