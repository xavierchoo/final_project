<div class="show">
	<div class="row">
		<div class="col-md-2">

  </div>
  <div class="col-md-5 cent">
		<% if @paragraph.present? %>
			<%= @paragraph.inner_html.html_safe %>
		<% elsif @pp.present? %>
			<%= @pp.inner_html.html_safe %>
		<% else %>
		<%end %>
	</div>

	</div>

</div>
