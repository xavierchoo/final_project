<% @articles.each do |article| %>
    <%= article.name %></br>
    <%= article.description %></br>
    <%= button_to "Add article", admin_selections_path(testing_id: article.id), method: :post %>
<% end %>
